<h1>Artists</h1>
<ul>
	<% @artists.each do |artist| %>
		<li>
			<p>
				<%= artist.name %>
				<% if artist.song_count > 0 %>
					<%= pluralize(artist.song_count, 'song') %>
				<% end %>
			</p>
		</li>
	<% end %>

</ul>
