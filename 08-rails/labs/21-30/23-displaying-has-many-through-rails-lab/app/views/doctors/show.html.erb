<h1><%= @doctor.name %></h1>
<ul>
  <% @doctor.appointments.each do |appointment| %>
    <li>Appointment: <%= appointment.appointment_datetime.strftime("%B %d, %Y at %H:%M") %>, patient: <%= link_to appointment.patient.name, patient_path(appointment.patient) %></li>
  <% end %>
</ul>
