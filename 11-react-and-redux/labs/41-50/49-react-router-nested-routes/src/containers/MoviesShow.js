// components that are directly connected to the store via the connect() function are considered 'container components'
import React from 'react';

const MoviesShow = (props) => {
  return (
    <div>
      <h3>Movies Show Component</h3>
    </div>
  );
};

export default MoviesShow;
