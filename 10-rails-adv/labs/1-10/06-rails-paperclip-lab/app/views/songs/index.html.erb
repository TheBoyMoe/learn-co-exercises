<ul>
  <% @songs.each do |song| %>
    <li><%= display_artist(song) %> - <%= link_to song.title, song_path(song) %><%= image_tag song.album_cover.url(:thumb) %></li>
  <% end %>
</ul>
<br>
<%= link_to "Add New Song", new_song_path %>
